name: Deployment
on: [push]

jobs:
  browser_extension:
    name: "Browser extension deploy"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: '18.12.1'

      - name: Install dependencies
        run: |
          yarn install

      - name: Build artifacts
        run: |
          yarn build:web
